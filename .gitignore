node_modules
dist
*.tsbuildinfo
*.local
.DS_Store
.env
.eslintcache
.azure
api/node_modules
local.settings.json
api/local.settings.json
.vscode/*
!.vscode/settings.json

# Local SQLite database (canonical data lives on your machine/NAS, not in git).
# The committed snapshot the static site builds from is public/data/ — note the
# leading slash: /data/ anchors to repo root only, so public/data/ is NOT ignored.
/data/
*.db
*.db-wal
*.db-shm

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
